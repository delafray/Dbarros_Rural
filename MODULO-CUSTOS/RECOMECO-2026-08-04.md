# 🔄 RECOMEÇO — estado, o que falta e onde buscar (04/08/2026, fim da janela)

> Palavras do usuário ao encerrar: **"comecei do modo errado — querendo o fim
> sem dar o início. O que temos hoje SERÁ aproveitado, mas vamos recomeçar."**
> Este arquivo é o mapa completo para a próxima sessão.

## ⚠️ PRIMEIRA PERGUNTA DA RETOMADA (antes de qualquer código)

O usuário NÃO definiu o que é "o início certo". Hipótese a validar com ele:
começar pelos **CADASTROS-BASE** (catálogo de produtos → biblioteca de espaços
com os 10 templates reais do rel. 78 → fornecedores → seções/categorias) e só
DEPOIS o fluxo de evento — em vez de começar pelo evento como fizemos.
**Perguntar: "o que é o início para você? me descreva a ordem certa de uso."**

## ✅ O QUE EXISTE E SERÁ APROVEITADO (não jogar fora nada disto)

- **Banco em produção**: 17 blocos aplicados — 25 tabelas, 76 policies RLS,
  triggers (baseline imutável, auditoria, anti-preço), busca "Mercado Livre"
  validada, seções (Julgamento›raças/Estrutura/Diversos), seeds (58 produtos,
  5 espaços, 23 categorias). Migrations: `supabase/migrations/20260804*.sql`.
- **Motor testado (250 testes, cobertura travada 90%)**: `utils/custosCalc.ts`
  (rateio com invariante, EAC, 3 gavetas), `utils/parseBR.ts`,
  `utils/checklistEvento.ts` (30 seeds condicionais), `utils/pasteTSV.ts`,
  `services/custosService.ts`, `services/custosXlsxService.ts` (xlsx travado
  ida-e-volta com round-trip testado).
- **UI v1** (a "carroceria" — pode ser refeita sem dó): `pages/CentroCusto.tsx`
  + `components/custos/*` (wizard 5 camadas, grade com seções/subtotais/Part.%
  auto-ajustável, cotações+mapa, pagamentos, espaços) + rota/menu.
- **Demo real**: Agroleite Perdizes 2024 no banco, batendo a planilha no
  centavo (R$ 348.211,22) — inclusive pegou R$ 16.000 de inconsistência do
  Excel original. Script: `scripts/seed-agroleite-perdizes-2024.sql`.
- **Branch `feature-modulo-custos`**: ~35 commits pequenos; NÃO mergeada.
- **Leitores de planilha**: `scripts/lerplanilha.mjs` (xlsx/ExcelJS) e
  `scripts/lerplanilha2.mjs` (xls legado/SheetJS, por índice — imune a acento).

## ⏳ O QUE AINDA FALTA

1. **Definir "o início certo"** com o usuário (pergunta acima) e redesenhar o
   fluxo/UX a partir dele — a v1 "não está nem perto do que ele imaginou"
   (dito em 04/08; os ajustes de grade foram paliativos).
2. **Seeds dos 10 templates padrão reais** (rel. 78: stand modular 18m²,
   pré-construído Box Truss 3 portes, pista de julgamento, pórtico, alameda,
   ilha, leilão, sala/VIP, bloco sanitário, baias) + ~20 itens novos de
   catálogo (Box Truss, lamicel, bagun, TS, Decorflex, HQI...). Bloco SQL
   pronto para gerar a partir do rel. 78.
3. **v2 registrada**: PDF ao cliente (RF-054, formato = planilha impressa),
   encerramento/snapshot + comparação entre anos + previsão 0,75→1→1,25
   (RF-037/046), sugestão de fornecedores por histórico (RF-030), UI do
   de-para "cadeira Amanda" (RF-045, service pronto), feed, curva ABC visual,
   fluxo de caixa semanal, importar planilhas históricas (Q-010).
4. **Merge na main** — só após o usuário aprovar.
5. Erro de TS pré-existente alheio ao módulo: `atendimentosService` (types
   desatualizados) — não é nosso, não bloquear por ele.

## 📚 ONDE BUSCAR INFORMAÇÃO (o mapa)

| Assunto | Onde |
|---|---|
| Protocolo de recarga de contexto | `MODULO-CUSTOS/LEIA-PRIMEIRO-RETOMADA.md` (ler PRIMEIRO) |
| Requisitos (55 RFs/14 RNFs) e decisões (~40, datadas) | `MODULO-CUSTOS/DOC-MODULO-CUSTOS/03` e `04` |
| Relatos do usuário na íntegra | `DOC-MODULO-CUSTOS/02-DOMINIO-EVENTO-RURAL.md` |
| Checklist Tela 1 / camadas | `DOC-MODULO-CUSTOS/05` · pontos cegos: `06` |
| Pesquisa (78 relatórios) | `MODULO-CUSTOS/PESQUISA-MODULO-CUSTOS/` — mapa temático no LEIA-PRIMEIRO |
| **Como o usuário monta espaços (96 planilhas)** | rel. **72–77** (lotes) e **78 (síntese — os 10 templates padrão)** |
| Planilhas reais de custos (3) | `C:\Users\ronal\OneDrive\Pictures\Screenshots\*.xlsx` + PDFs ao cliente |
| **96 planilhas de espaços/propostas** | `C:\Users\ronal\OneDrive\Pictures\Screenshots\Nova pasta\` (ler com `scripts/lerplanilha2.mjs --lista/--pasta/--i`) |
| Sistema-mãe do descritivo | `C:\Users\ronal\Documents\Antigravity\PROSPERITAS` (port RF-031) |
| Regras de trabalho com o usuário | `DOC-MODULO-CUSTOS/00-INDICE.md` (7 regras) + memória persistente |

## Como retomar

Nova janela → dizer **"vamos voltar ao nosso novo módulo"** → a IA lê o
LEIA-PRIMEIRO (que aponta para este arquivo) → faz a PERGUNTA DO INÍCIO →
segue com o redesenho aproveitando tudo acima.
